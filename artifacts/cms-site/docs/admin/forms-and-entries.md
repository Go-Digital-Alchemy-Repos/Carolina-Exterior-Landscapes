# Forms And Entries

Forms are generic CMS assets. They can be embedded in page blocks, assigned to sidebar widgets, or opened through standalone `/forms/:slug` links.

Each form controls its fields, submit button text, success message, admin notifications, and whether entries should also appear in the contact inbox.

Forms no longer create CRM records or sync to external newsletter systems.

# Media Library And Assets

The media library stores image and document assets used by CMS pages, reusable sections, and SEO settings.

Use alt text, captions, and descriptions when assets appear in public content. Before deleting a media item, review its usage list so live CMS pages do not keep broken references.
